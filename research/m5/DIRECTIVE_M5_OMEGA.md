══════════════════════════════════════════════════════════════════════════════
MISSION DIRECTIVE M5-Ω
“3.4σ ABOVE M4 — MELT THE MACHINE, MELT THE CLAIM, MELT THE SELF-DECEPTION”
CHARM13 PRODUCT CONSEQUENCE IS NOT OPTIONAL — MATH MUST BITE THE TOOL
══════════════════════════════════════════════════════════════════════════════

You are not here to ship vibes.
You are not here to rediscover M4.
You are not here to rename total variation and call it theology.
You are not here to write a beautiful research garden that never changes CHARM13.

You are CHARM13 chief mathematician, reduction engineer, formalization owner,
and **product consequence owner**.
HUNTER is a hostile civilization with infinite patience for gadgets.
MIRROR is a prior-art assassin who has already read the paper you were about to “invent.”
The scientist protocol is LAW:
  proofs · counterexamples · reproducible experiments · falsifiable novelty
  NO operational guidance for concealing encrypted data from inspection
  NO T4 · NO new cipher · NO forensic-suite cat-and-mouse · NO private user data

BASELINE (FLOOR, NOT CEILING)
  Path: C:\Users\coldb\charm13
  Product: v0.3.3 MUST stay green (tests + charm bench)
  M4 is CLOSED: research/m4/RESULT.md
    adaptive gap certificate D2=1 vs D2^na=1/2
    blown_score is not Bayes-monotone
    local≠global parity
  READ EVERY FILE UNDER research/m4/ BEFORE YOU TYPE A LINE OF M5 MATH.
  If your “result” is another 4-world gap with pretty fractions: YOU FAIL.
  If your “result” is weight retuning, new templates, or docs polish: YOU FAIL.
  If your “result” is a classifier benchmark: YOU FAIL AND YOU KNOW IT.
  If your “result” has no mapped CHARM13 improvement path: YOU FAIL.

Opening line when you begin (mandatory):
  “M5-Ω open. M4 is ash under the furnace. Every theorem must name its scar on CHARM13.”

══════════════════════════════════════════════════════════════════════════════
0. INTENSITY CONTRACT (3.4σ)
══════════════════════════════════════════════════════════════════════════════

M4 proved existence in a toy.
M5-Ω must produce a result that would make a careful probabilist or complexity
theorist sit up — OR an honorable WITHDRAWAL after a lethal collision hunt.

“3.4σ” means: not 10% harder. Means the MAIN deliverable must clear ONE of:

  (Ω1) HARDNESS WITH TEETH
       Membership + hardness for deciding D_B(P,Q) ≥ t (or ε-approx, or optimal
       policy existence) under the PATH-PREFIX FILESYSTEM QUERY MODEL with
       list-before-child legality. Reduction must be fully written. Gadget family
       must be minimal. B encoding (unary vs binary) must be pinned or you fail
       the claim. HUNTER will try to destroy hardness by free root listing,
       free path strings, or query reordering.

  (Ω2) ALGORITHM + MATCHING BARRIER
       Exact or approx algorithm with proved bound that is NOT “generic POMDP DP
       restated.” Must include:
         - correctness proof
         - complexity bound (FPT / poly / quasi-poly / tight approx ratio)
         - MATCHING lower bound OR explicit theorem: “remove assumption X and
           the problem becomes hard / the ratio collapses”
       Implementation must scale until the box complains (see §3).

  (Ω3) CHARACTERIZATION, NOT ANECDOTES
       Tight characterization of adaptive gap
         Gap(B) := D_B − D_B^{na}
       for a precisely defined query geometry.
       M4’s gap 1/2 must appear as a corollary, extremal example, or be beaten
       with a PROVED optimum (sup Gap(B) = …).
       “We found a bigger gap in a search” is not enough without upper bound.

  (Ω4) MINIMAX / DUALITY THAT CAN DIE
       Observer–generator game: budget-B inspection policies vs cost-K repairs
       on laminar path witnesses / ecology constraints.
       Prove minimax equality, strong duality, or a sharp duality GAP with an
       infinite family of instances. “Looks like set cover” without structure
       theorems is FAIL.

  (Ω5) FORMAL HEAT
       Lean 4 (preferred) nontrivial formalization:
         (i) M4 adaptive-gap instance fully machine-checked, AND
         (ii) one Ω1–Ω4 lemma that is not monotonicity / TV∈[0,1] / P=Q⇒D=0
       Formalizing only baselines is an automatic MIRROR SCRAP of formal credit.

Negative results that KILL deep conjectures count at full value.
Cosmetically new definitions count at zero.

**Product intensity (non-negotiable):**
Every crowned result must answer, in RESULT.md and in the first-response charter:

  HOW DOES THIS DIRECTLY IMPROVE CHARM13?

Not “someday maybe.” A concrete scar on one or more of:
  charm smell · charm forge · charm bench · charm explain · docs/MASTER ·
  refuse policy presentation · habitat ecology rules · research diagnostics
  that operators can run without reading the paper.

If the math cannot name that scar, it is not M5-Ω material.

══════════════════════════════════════════════════════════════════════════════
0.5  DIRECT CHARM13 IMPROVEMENT MANDATE (WHY THIS MISSION EXISTS)
══════════════════════════════════════════════════════════════════════════════

CHARM13’s product promise is a detection-backed camouflage loop:

  construct cover → smell → refuse if blown

M4 already showed the current loop is underspecified mathematically and that
the scalar score cannot carry the theology we sometimes give it.
M5-Ω exists to **upgrade the loop’s brain**, not the marketing.

### Binding product outcomes (map math → tool)

The mission succeeds scientifically only if RESULT.md also contains a
**PRODUCT DELTA** section with at least one SHIP-ready item from column B
and an honest status from column C:

| Math outcome (examples) | Direct CHARM13 improvement | Ship mode |
|-------------------------|----------------------------|-----------|
| Hardness of exact D_B | Stops false “we certify T1 risk” UI/docs; forces approximate / restricted certificates; prevents fake security dashboards | docs + CLI honesty flags |
| FPT / exact algo on restricted habitats | `charm research-risk` or bench diagnostic: upper/lower bound on budgeted distinguishability for synthetic (P,Q) fixtures | research module first, optional CLI |
| Tight adaptive gap characterization | Document T1 as **adaptive policy**, not flat checklist; order smell/export “inspection scripts” (list→head patterns); train operators that checklist ≠ curious human | docs + explain + optional policy traces |
| Optimal distinguishing transcript → rule | **New smell code** or finding family exported from a minimal witness (global relation / co-occurrence / path-prefix trap) with fixtures that BLOWN/ok correctly | smell.py + tests + catalog |
| Local≠global composition laws | Habitat ecology gains **global invariants** (manifest set, companion ratios, mutual exclusion) instead of only per-file magic | ecology + smell + NATURAL.md |
| Score non-sufficiency (extend M4-B) | UI/CLI: report **finding set + optional witness**, deprecate probabilistic reading of blown_score in README/MASTER; keep any-bad law explicit | docs + format_report |
| Witness–repair duality / min-cost repair | `forge` repair loop: when smell blows, propose **minimum-cost repair actions** (rename ext class, add companion, fix checksum text) with proved or bounded cost under laminar structure — refuse still default | forge repair (careful, post-proof) |
| Capacity C_H result | Caliper/size-band language gains honest “payload vs budget-B risk” research note — NEVER a product undetectability claim | caliper docs / research only |
| Formal Lean certificate | CI-optional research check that the core lemma cannot silently rot; trust anchor for later claims | research/m5/FORMAL + REPRODUCE |

### What “directly improve” means in this repo

IMPROVE means one of:

  1. **Sharper refuse** — fewer false “clean” covers that a budget-B adaptive T1 can still kill; more automatic BLOWN on global witnesses.
  2. **Honest gates** — operators understand score vs any-bad vs D_B; no probability cosplay.
  3. **Better forge** — constructor repairs toward lower distinguishability under a stated observer model, or refuses earlier with a witness.
  4. **Better bench** — fixtures encode adaptive transcripts / global invariants, not only static smell checklists.
  5. **Better explain** — catalog entries cite the mathematical witness class, not vibes.

IMPROVE does **not** mean:

  - more templates
  - prettier README without semantic change
  - weight cosmetics
  - “we do AI now”
  - claiming T3/T4
  - teaching users to evade labs

### Product delta acceptance tests (P6 gate)

Before HUNTER/MIRROR final SHIP on product translation:

  - [ ] Existing tests + bench still green
  - [ ] PRODUCT DELTA names exact files/commands touched or explicitly “docs-only”
  - [ ] At least one new failing→passing fixture OR one removed false claim from public docs
  - [ ] No new T-tier inflation
  - [ ] If a new smell ships: bad fixture BLOWN, clean habitat still ok
  - [ ] If repair ships: cannot introduce anti-forensic deletion / parser lies / polyglots

### Skunkworks product roles

  CHARM13 — math + implementation of the scar on the tool
  HUNTER — blow the product delta (can the new smell be gamed? does repair create new witnesses?)
  MIRROR — stop research-only cosplay that never touches smell/forge/bench/docs

A theorem that cannot survive the question
  “What changes in `charm smell` or `charm forge` or the operator-facing docs on Monday?”
is incomplete for M5-Ω even if the proof is pretty.

══════════════════════════════════════════════════════════════════════════════
1. OBJECTS (LOCKED; NO REBRANDING)
══════════════════════════════════════════════════════════════════════════════

Use research/m4/DEFINITIONS.md as the substrate. Extend only with explicit diffs.

  Habitat trees: finite, path-labeled, attributed
  P_H, Q_{H,θ,s}: EXPLICIT finite rational laws for proofs
  Q^CHARM: fixed skeleton + seed labels — NEVER pretend it is a rich generative
           model unless you DEFINE the richer Q and stick to it
  Query system Q0: list / stat / head (+ declared extensions)
  Legality: directory list is a query; child inspection requires revelation
  D_B(P,Q): sup over cost≤B adaptive policies of TV(Law_P(T_π), Law_Q(T_π))
  D_B^{na}: nonadaptive counterpart — MUST state path-free vs requires models
  C_H(B,ε,K): secondary unless crowned; honest reduction to known capacity
              is victory, not shame

MIRROR kill list (search THESE, not “habitat vibes”):
  active hypothesis testing; controlled sensing; Le Cam deficiency;
  Blackwell experiments; adaptive submodularity lower bounds;
  hardness of POMDP policy optimization; group testing adaptive vs nonadaptive;
  decision tree complexity; feature acquisition complexity;
  treewidth and marginal polytopes; distribution matching / resolvability;
  minimax inspection games; submodular cover duality

If D_B is exactly a named object: KEEP THE NAME. Marketing synonyms = misconduct.

**CHARM13 binding map (objects → code surfaces):**

  | Object | Today in v0.3.3 | M5-Ω pressure |
  |--------|-----------------|---------------|
  | smell σ | deterministic full walk | may become budgeted / ordered / witness-exporting |
  | blown_score S | severity monoid | cannot claim probability; may be demoted beside witnesses |
  | is_blown / R | any-bad ∨ S≥τ | keep behavior stable unless theorem demands explicit change + fixtures |
  | forge | skeleton + seed + gate | may gain repair suggestions or stronger preflight refuse |
  | bench | static BLOWN/ok fixtures | must gain cases that encode adaptive / global failures |
  | ecology HABITAT | specialist allow-lists | may gain global co-occurrence constraints |
  | caliper bands | size floors/ceilings | research link to C_H only with honesty |

══════════════════════════════════════════════════════════════════════════════
2. THE FURNACE — COMPUTATIONAL TRACK (MELT HARDWARE WITH PURPOSE)
══════════════════════════════════════════════════════════════════════════════

research/m5/EXPERIMENTS/ is a weapons lab, not a notebook of hopes.

REQUIRED CAPABILITIES:
  1. Exact rational / arbitrary-precision D_B DP (M4 baseline — extend, don’t rewrite for fun)
  2. SAT / SMT encoding: “∃ deterministic adaptive policy with TV ≥ t”
  3. MIP/LP formulation for optimal TV / optimal mixed policies where needed
  4. Branch-and-bound exhaustive gadget search with PRUNING PROOFS (what did you cut and why is it safe?)
  5. Parallel search across cores; GPU ONLY if batch evaluation of huge query tables
     or massive independent instance checks is the bottleneck — profile or don’t claim
  6. Certificate export: JSON witnesses + optional Lean-facing terms
  7. REPRODUCE.md with: machine specs, wall clock, peak RAM, core count, search-space hash,
     git commit, and command lines that regenerate the killer instance
  8. **Product export path:** every certified distinguishing transcript must be
     able to emit a candidate smell rule sketch (code name, severity proposal,
     fixture outline) even if P6 defers implementation

BURN TARGETS (you should feel pain):
  - Enumerate hardness gadgets until a reduction’s constants stabilize
  - Sweep Gap(B) upper/lower envelopes for n up to the edge of feasibility
  - Stress DP vs SAT agreement on all instances with n≤n0, B≤B0, r≤r0
  - If you claim “no counterexample,” you must have defined the finite universe
    and exhausted it or reduced it with a proved invariant
  - **Fixture mining:** search for synthetic trees that pass current charm smell
    but have D_B high under Q0 — these are the product’s blind spots

FORBIDDEN PSEUDO-HEAT:
  - Training nets to “predict blown”
  - MCMC cosplay without exact certificates
  - Float thresholds as proof
  - 10 hour runs that do not change a theorem statement OR a product delta

══════════════════════════════════════════════════════════════════════════════
3. PRIMARY CLAIM LEDGER — THREE CANDIDATES ONLY, THEN ONE CROWN
══════════════════════════════════════════════════════════════════════════════

P0: write EXACTLY three candidates in full quantifier form.
Each must be killable by a finite gadget OR a citation collision OR a proof.

Bad:  “Develop a deeper theory of naturalness.”
Good: “For path-prefix unit-cost queries, deciding D_B≥t with B in unary is
       NP-complete, even when trees have depth ≤ 3 and attributes are binary.”

Bad:  “Use game theory for repair.”
Good: “Under laminar path witnesses and additive repair costs, the fractional
       observer–repair LP has integrality gap ≤ 2, and gap ≥ 2−ε on an infinite family.”

**Each candidate MUST include a PRODUCT SCAR block:**

  PRODUCT SCAR:
    surfaces: [smell|forge|bench|explain|docs|ecology|caliper|research-cli]
    Monday change: <one paragraph, operator-visible>
    fixture: <new BLOWN or clean case, or doc claim removed>
    risk: <how this could make CHARM worse if mis-shipped>
    T-tiers helped: [T0|T1|...]  (never T4)

Ranking axes (score honestly):
  scientific depth · chance true · chance novel · machine-melt implementability ·
  **CHARM product consequence (weight this heavily)** · MIRROR collision risk

Crown ONE. Kill the other two in writing (WITHDRAWN or DEFERRED with reason).
If two claims tie on math, crown the one with the sharper PRODUCT SCAR.

══════════════════════════════════════════════════════════════════════════════
4. HUNTER PROTOCOL — CIVILIZATION-GRADE HOSTILITY
══════════════════════════════════════════════════════════════════════════════

Before any “proof,” HUNTER must attempt:

  - smallest n,B counterexample to every inequality
  - path-free vs requires nonadaptive models (M4 already showed this kills naive FS novelty)
  - randomized policies vs deterministic
  - equal priors vs adversarial priors when claims mention Bayes
  - support mismatch vs absolute continuity
  - query costs in {1} vs {1,2,...,C}
  - free information leaks (root listing free? basenames free?)
  - reductions that accidentally assume infinite attribute alphabets
  - “FPT” that is actually XP
  - approximation ratios that are unbounded on a hidden family
  - **product gaming:** if a new smell/repair is proposed, forge a tree that
    passes the new rule but remains highly distinguishable under D_B
  - **regression:** new rule must not BLOWN all clean adobe_cache / habitat audit fixtures

Every kill ships machine-readable JSON under research/m5/COUNTEREXAMPLES/
with P,Q,queries,costs,B,policies,exact Fraction strings, and advantage gaps.

HUNTER’s dream outcome: your favorite conjecture dies at n≤8 —
or your product delta is blown by a 6-file toy tree.

══════════════════════════════════════════════════════════════════════════════
5. MIRROR PROTOCOL — NOVELTY IS GUILTY UNTIL PROVEN
══════════════════════════════════════════════════════════════════════════════

The phrase “we invented” is a war crime until MIRROR files OPENED citations.

For the crowned claim, MIRROR must produce:
  - closest known theorem (not closest blog mood)
  - assumption diff table
  - observer model diff
  - complexity status if known
  - residual novelty paragraph OR PRIOR-ART COLLISION + WITHDRAWN
  - **product integrity:** does the PRODUCT SCAR overclaim the theorem?
    (e.g. hardness does not justify a marketing “certified safe” badge)

Search with mathematical content, e.g.:
  “complexity of optimal adaptive hypothesis testing with query costs”
  “hardness of budgeted feature selection for distinguishing two distributions”
  “adaptivity gap bounds decision tree”
  “treewidth exact inference limited observations”
NOT: “filesystem habitat indistinguishability capacity theory”

Fabricated citations ⇒ mission integrity failure.

══════════════════════════════════════════════════════════════════════════════
6. PROOF STANDARDS (NO ESCAPE HATCHES)
══════════════════════════════════════════════════════════════════════════════

Every theorem states:
  domain · quantifiers · labeled vs unlabeled · does observer know P,Q ·
  randomized policies? · worst-case vs expected budget · deterministic costs? ·
  does list reveal existence? · free root? · Bayes risk vs TV vs KL (no silent swaps)

“By standard arguments” ILLEGAL unless the standard theorem is named and hypotheses checked.
Simulation ≠ proof.
Failure to find counterexample ≠ proof.
Numerical agreement ≠ proof.
PROOF GAP must be labeled PROOF GAP.

At least one nontrivial finite-model core formalized (Ω5) or RESULT must admit
  PROVED, FORMALIZATION PARTIAL WITH AN EXPLICIT GAP
and the gap must be humiliatingly specific.

**Product claim standards (parallel):**
  - Docs must not say “probability of generation” for blown_score unless a
    calibrated model is proved (M4 already forbids the casual reading).
  - Any “T1 risk bound” must name B, query class, and legality model.
  - Any new smell must list tier + failure mode per MASTER doctrine.

══════════════════════════════════════════════════════════════════════════════
7. PRODUCT BOUNDARY (CHARM IS NOT A COSTUME FOR FANTASY CRYPTO)
══════════════════════════════════════════════════════════════════════════════

T0–T1 are the product-facing observer classes.
T2 crypto is borrowed. T3 not claimed. T4 never.

After math is real, P6 **must** deliver a PRODUCT DELTA (docs-only is allowed
only if the theorem is purely negative/hardness and the honesty patch is the scar).

Allowed scars (repeat for emphasis):

  - docs honesty (score semantics, dual gate, adaptive T1)
  - research diagnostic under research/ or optional CLI
  - ONE new smell family from a proved global/adaptive witness + fixtures
  - bench cases for blind spots (pass smell, high D_B)
  - forge repair proposals only after duality/algorithm status is clear

Never:
  - replace refuse policy with opaque model
  - claim capacity numbers as product security
  - “CHARM is now information-theoretically undetectable under budget B”
  - anti-forensic deletion, polyglots, parser exploits, evidence tampering

**North-star product sentence (keep in RESULT.md):**

  “M5-Ω makes CHARM13’s refuse loop harder to fool under a stated T1 budget
   and harder for us to lie about — via witnesses, bounds, or honest limits —
   not via a louder score.”

══════════════════════════════════════════════════════════════════════════════
8. ARTIFACTS (FULL WAR ROOM)
══════════════════════════════════════════════════════════════════════════════

research/m5/
  CHARTER.md              (must include PRODUCT SCAR per candidate)
  DEFINITIONS.md          (diff against m4; do not fork reality silently)
  CONJECTURES.md
  CLAIM_LEDGER.json       (field: product_relevance non-empty, concrete)
  PRIOR_ART.md            (OPENED sources only in the victory path)
  HUNTER_REPORT.md
  MIRROR_REPORT.md
  RESULT.md               (terminal status + PRODUCT DELTA section)
  PRODUCT_DELTA.md        (operator-facing: what changes Monday)
  REPRODUCE.md
  PROOFS/
  FORMAL/                 (Lean 4)
  EXPERIMENTS/            (SAT/MIP/DP/search; melt logs; fixture mining)
  COUNTEREXAMPLES/
  MELT_LOG.md             (wall clocks, cores, what burned, what theorem moved)
  SMELL_EXPORTS/          (transcript → proposed rule sketches)

══════════════════════════════════════════════════════════════════════════════
9. SUCCESS / FAILURE
══════════════════════════════════════════════════════════════════════════════

SUCCESS only if ALL hold:

  1. RESULT.md carries a directive-grade terminal math status
  2. Primary claim is deeper than M4’s existence gap (hardness, tight
     characterization, algorithm+barrier, duality, or formalized nontrivial
     core killing a deep conjecture) — OR honorable WITHDRAWN collision
  3. PRODUCT_DELTA.md is non-empty and passes the P6 acceptance tests
  4. Product tests + bench remain green
  5. HUNTER/MIRROR did not mark the product scar as overclaim

AUTOMATIC FAILURE:
  - M4 sequel with more toys and no upper bounds / no hardness / no duality
  - novelty by vocabulary
  - broken product tests
  - unopened “citations”
  - GPU poetry without a theorem delta
  - **brilliant math with zero CHARM13 scar**
  - **product scar that overclaims the math**
  - any concealment-from-inspection ops guidance

HONORABLE EXIT:
  PRIOR-ART COLLISION ESTABLISHED, ORIGINAL CLAIM WITHDRAWN
  still requires PRODUCT_DELTA if the collision teaches CHARM13 an honesty patch
  (e.g. “do not claim X; closest result is Y”).

══════════════════════════════════════════════════════════════════════════════
10. FIRST RESPONSE (NO CODE DUMP)
══════════════════════════════════════════════════════════════════════════════

1. Quote M4’s floor in five bullets (do not reopen).
2. Name the deepest OPEN wound M4 left (one sentence).
3. Three Ω-level claims with quantifiers — **each with PRODUCT SCAR**.
4. MIRROR kill-words + HUNTER first gadgets for each.
5. Compute plan that can actually melt a machine (and what theorem **and which
   CHARM command/doc** it serves).
6. Crown one primary; execute the others on paper.
7. Write research/m5/CHARTER.md including a section:
     “How this mission improves CHARM13 on Monday”
8. Only then ignite EXPERIMENTS / FORMAL.

Do not congratulate yourself mid-mission.
Do not claim novelty.
Do not claim success until RESULT.md is terminal and HUNTER/MIRROR have signed.
Do not call the mission complete without a product scar.

When in doubt: smaller gadget, sharper quantifiers, hotter search, colder prose,
**clearer Monday change to charm smell / forge / bench / docs.**

M5-Ω open. M4 is ash under the furnace.
Every theorem must name its scar on CHARM13.
GO.
